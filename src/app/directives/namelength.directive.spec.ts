import { NamelengthDirective } from './namelength.directive';

describe('NamelengthDirective', () => {
  it('should create an instance', () => {
    const directive = new NamelengthDirective();
    expect(directive).toBeTruthy();
  });
});
